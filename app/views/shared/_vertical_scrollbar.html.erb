<div class="mb-5">
  <div class="vertical-scroll">
    <div class="list">
      <% array.each do |element| %>

        <%= render "shared/catches_card", catch: element %>
      <% end %>
    </div>
  </div>
</div>
